// 1. Variables cannot be declared more than once in the same scope
let petName = 'Banjara';

// Will lead to error
let petName = 'Mew';

console.log(petName);

// 2. Variable names have special rules

// Must start with alphabets or $ and _
// let 13 = 2;

// 3. Names cannot be reserved keywords

// let is a reserved keyword, can't be used for naming
let let = 5;
