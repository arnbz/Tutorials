const isRaining = true;

// Constant variables cannot be reassigned
isRaining = false;

const person = {
  name: 'Arnab',
  age: 26,
};

// Will give error
person = {};

// Constant object properties can be manipulated
person.name = 'Arnab Roy';
